name: 1-my first workflow
on:
  workflow_dispatch:
jobs:
  my-first-job:
    name: my first job
    runs-on: ubuntu-latest
    steps:
      - name: step-1 say hello
        run: |
          sleep 10
          echo '************ welcome to first workflow **************
      - name: step-2 print whats the date today?
        run: date
      - name: step-3 print ubuntu version
        run: lsb_release -a
  my-second-job:
   name: my second job
   runs-on: ubuntu-latest
   steps:
     - name : first step - print current directory
       run: |
         sleep 10
         pwd
        
      
